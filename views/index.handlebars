<div class="container">
<h1>Fat Burger</h1>

<h3>What's on your burger wish list?</h3>

<ul>
  {{#each burgers}}
  	<li>
       <h3>Burger: {{this.burger}} <button type="button" class="btn btn-danger" id="{{this.id}}">Eat Me!</button></h3>
  	</li>
  {{/each}}
</ul>

<div>
  <form action="/api/burgers" method="POST">
  	<textarea type="text" name="burger"></textarea>

  	<button type="submit">Submit</button>
  </form>
</div>
</div>